# Supabase Configuration (Frontend)
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Supabase Configuration (Backend - if using server)
SUPABASE_URL=your_supabase_project_url
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Server Configuration (optional)
VITE_API_URL=http://localhost:3001

# To get these values:
# 1. Create a project at https://supabase.com
# 2. Go to Settings > API
# 3. Copy the Project URL and anon/public key for frontend
# 4. Copy the service_role key for backend (keep this secret!)

# If you don't have Supabase configured, the app will run in demo mode
# with localStorage for data persistence